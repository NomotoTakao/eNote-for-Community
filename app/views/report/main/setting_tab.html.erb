<input type="hidden" id="admin_flg" value="<%= current_m_user.admin_flg %>" />
<div id="setting_contents" />

<script type="text/javascript">
  <!--
    $(document).ready(
      function(){
        var admin_flg = $("#admin_flg").val();
        var url = "";
/*
        if(admin_flg==1){
          url = "/report/main/setting_admin";
        }else{
*/
          url = base_uri + "/report/main/setting_superior";
//        }
        // 日報検索タブのコンテンツをクリアする。
        // 1) <a>タグのうち、内容が「日報検索」である要素のhref属性求める。
        tab_id = $("a:contains('日報検索')").attr("href").replace("#", "");
        // 2) idが{tab_id}の要素の内容をクリアする。
        $("#" + tab_id).html("");
        // 設定タブの内容をロード
        $("#setting_contents").load(url);
      }
    );
  //-->
</script>
